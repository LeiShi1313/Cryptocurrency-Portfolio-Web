import { RemoveSmallPipe } from './remove-small.pipe';

describe('RemoveSmallPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSmallPipe();
    expect(pipe).toBeTruthy();
  });
});
